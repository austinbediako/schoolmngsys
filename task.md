# task.md — Current Execution State

> Updated: 2026-07-31 · Milestone status lives here; project knowledge lives in [memory.md](memory.md); canonical specs in [docs/](docs/README.md); session bootstrap in [CONTEXT.md](CONTEXT.md).

## Current Milestone

**Phase 1 — MVP Backend COMPLETE (M1, M2, M3, M4, M5 CLOSED).** All planned work packages (WP-0 through WP-10) plus bonus scope (BECE, admin-bootstrap, audit query, CSV export) are complete and verified with 95/95 green tests.

## Current Objective

Phase 1 MVP Backend complete. Ready for Phase 2: Frontend Next.js Web Applications (staff-console, guardian-portal) and deployment configuration.

## Completed

- [x] Spring Boot 4.1 skeleton (pom, application.yml, docker-compose, .env.example)
- [x] Full documentation set under `docs/` (vision, glossary, domain, roles, rules, FRs, NFRs, workflows, modules, data, API, security, ADR-001…010, gap analysis, roadmap)
- [x] memory.md, task.md, CLAUDE.md (engineering constitution)
- [x] **Stakeholder confirmation of all assumptions A-01…A-11 (2026-07-29)** — defaults are now binding
- [x] Phase 1 implementation plan: 11 work packages, 5 milestones, 14-migravvtion sequence, permission catalog, per-package test plan
- [x] **CONTEXT.md** established (session bootstrap protocol, knowledge-ownership registry, maintenance rules) + ADR-011 recording why
- [x] Self-healing sweep: fixed the false "contexts map 1:1 to modules" claim, gave FR-LMS-01 a module home (`lms`), added concept-document → architecture traceability ([docs/08 §6](docs/08-module-architecture.md#6-traceability))
- [x] **WP-0 shared foundations (2026-07-29)**: `BaseEntity` (UUIDv7 via Hibernate `@UuidGenerator`, created/updated at/by, `archived_at`, `version`); RFC 7807 `GlobalExceptionHandler` + `ProblemType` catalog + `traceId` filter; Bean Validation wired at the DTO boundary (validation + constraint-violation handlers); in-process `DomainEvent`/`DomainEventPublisher`; audit interception (`shared.audit.Audited` marker + `audit` module's `AuditAspect`/`AuditRecorder`, `MANDATORY` propagation so it fails loudly outside a transaction); pagination envelope (`PageResponse`, `PageRequestFactory`, cap 100); `V1__shared_foundations` migration (pgcrypto, `audit_log`); Testcontainers (real Postgres, never H2) + ArchUnit module-boundary tests. Implementation notes: [memory.md](memory.md#wp-0-shared-foundations--implementation-notes).
- [x] **WP-1 auth (2026-07-29)**: `Account`/`Role`/`Permission`/`AccountRole`/`RefreshToken`/`LoginAttempt`/`PasswordResetOtp` entities; `V2__auth` + `V3__seed_permissions_and_roles` migrations (full permission catalog + 10 roles seeded per docs/03); JWT issue/parse (JJWT 0.12.6, HS256), stateless `SecurityConfig` + `JwtAuthenticationFilter` + permission-string method security (`@PreAuthorize`); login/refresh-rotation/logout with lockout (FR-AUTH-05) and immediate refresh-revocation on deactivation (FR-AUTH-04); OTP password reset (FR-AUTH-03, `LoggingOtpSender` stub pending WP-8); account provisioning + role assignment (BR-SE-003); `CurrentAccountProvider`/`AuthenticatedAccount` scope-filter mechanism in `shared.security` for WP-2+ to build on. All 19 tests green (login/lockout/refresh-rotation/deactivation-revocation, permission-gate 401/403/200, scope-filter unit test, WP-0 regression, ArchUnit, contextLoads); app verified end-to-end against compose Postgres (health 200, malformed body → 400, bad creds → 401 RFC 7807, unauthenticated protected endpoint → 401). Implementation notes: [memory.md](memory.md#wp-1-auth--implementation-notes).
- [x] **WP-2 academics (2026-07-29)**: `AcademicYear`/`Term`/`TermCalendarVariant`/`SchoolDayException`/`Department`/`ClassLevel`/`Subject`/`SchoolClass`/`ClassTeacherAssignment`/`ClassSubjectOffering` entities; `V4__academic_reference_data` (seeded N1-B9 ladder, 4 departments, 11 NaCCA subjects) + `V5__academic_structure` migrations; year/term lifecycle with the three-terms invariant and one-active-year-at-a-time (BR-AS-001), term calendar variant resolution for JHS 3's earlier close (BR-AS-003), school-day computation layering weekends/holidays/term-range (FR-ACAD-02), class + one-class-teacher-per-year in both directions (BR-AS-005/A-01), subject offerings with level-applicability and teacher assignment (BR-AS-006). Also fixed a real WP-0 gap: `GlobalExceptionHandler` never had a `DataIntegrityViolationException` → conflict handler despite docs/10 §3 requiring it. All 28 tests green; app verified against compose Postgres (migrations apply clean, permission-gated endpoints correctly 401 unauthenticated). Implementation notes: [memory.md](memory.md#wp-2-academics--implementation-notes).
- [x] **WP-3 people (2026-07-30)**: `Student`/`Guardian`/`StudentGuardian`/`StudentDocument`/`Staff`/`StaffQualification` entities; `V6__people` migration; BR-EN-004 (guardian + primary-contact requirement) enforced atomically at student creation and re-checked on guardian-link removal; `GuardianWardResolutionService` — the named authorization scope source every future guardian-scoped module will call; student number generation (BR-EN-002/A-05, `UBS-<year>-<seq>`, immutable, zero-padded); staff registry + qualifications (BR-ST-001/002); document storage decision made (filesystem behind a `DocumentStorage` seam, docs/14 §8 resolved) with access-checked upload/list/download endpoints. All 38 tests green; app verified against compose Postgres (migration applies clean, permission-gated endpoints correctly 401 unauthenticated). Two things deliberately deferred and documented rather than invented: no auth-account-deactivation cascade on `endEmployment` (no such domain event exists yet), and BR-ST-001's qualification-before-assignment rule isn't cross-module-enforced (academics has no dependency on people in the module boundary diagram). Implementation notes: [memory.md](memory.md#wp-3-people--implementation-notes).
- [x] **WP-4 enrollment (2026-07-30)**: `Enrollment` entity (plain UUID refs to student/class/year, not cross-module JPA associations); `V7__enrollment` migration; BR-EN-001 (one active enrollment per student per year, service check + partial unique index); exit handling (BR-EN-005: reason+date required, `TRANSFERRED`/`WITHDRAWN`) that syncs `people.Student.status` via two new `StudentService` methods added this WP; roster query (ACTIVE only) and full history query (everything) — added `get(id)` to `AcademicYearQueryService`/`ClassQueryService` to support this. All 44 tests green; app verified against compose Postgres. **This closes both M1 (walking skeleton) and M2 (registry ready)** — academics/people were built out fully, not just a thin M1 slice. Deliberately not built: the FR-STU-04 transfer-summary document export (no template/PDF library chosen, not in the WP-4 test plan) and a first-admin-account bootstrap (flagged for before M5). Implementation notes: [memory.md](memory.md#wp-4-enrollment--implementation-notes).
- [x] **WP-5 attendance (2026-07-30)**: `AttendanceRecord` entity keyed by `enrollmentId` (real FK this time, unlike the opaque cross-module UUIDs elsewhere) + `attendance_date`; `V8__attendance` migration; bulk register submission with school-day validation (via WP-2's `SchoolDayQueryService`) and duplicate-submission rejection (BR-AT-001/002); same-day self-correction vs elevated-permission past-date correction with mandatory reason (BR-AT-004); A-07 LATE/EXCUSED aggregation (`AttendanceSummaryService`). Introduced an injectable `Clock` bean (`shared.config.ClockConfig`) so the "only today's register" rule is deterministically testable regardless of which real-world day the suite runs on. All 50 tests green; app verified against compose Postgres. **This completes WP-5, the only actionable half of M3** — WP-8 (communication, M3's other piece) still waits on WP-6/WP-7. Implementation notes: [memory.md](memory.md#wp-5-attendance--implementation-notes).
- [x] **WP-6 assessment (2026-07-30)**: `GradeScale`/`GradeBand`/`AssessmentComponent`/`Score`/`TermResult`/`ReportCard` entities; `V9__assessment` + `V10__seed_default_grade_bands` migrations (the latter deviates from the original docs/14 §4 plan — see below); weighted-total computation (SBA/EXAM per A-03, grade lookup per A-04), BR-AA-007 all-or-nothing missing-score block, BR-AA-001 component-weight-sum block, A-12 exempted/N-A renormalization (newly recorded, proposed-not-confirmed assumption), BR-AA-004 competition-ranking subject *and* class positions, BR-AA-003 DRAFT→SUBMITTED→HOD_APPROVED→PUBLISHED pipeline (subject-scoped approval, class-scoped publication per WF-04), BR-AA-006 immutable-publish + versioned revision. Fixed a real Hibernate flush-ordering bug the tests caught (`ResultRevisionService` briefly violated a partial unique index). All 66 tests green; app verified against compose Postgres. Two docs/14 plan corrections made along the way (self-healing, not deviation): `V10` seeds a year-independent template rather than an impossible "first year" row, and A-12 was recorded rather than invented. Implementation notes: [memory.md](memory.md#wp-6-assessment--implementation-notes).
- [x] **WP-7 finance (2026-07-30)**: `FeeSchedule`/`FeeItem`/`Invoice`/`InvoiceLine`/`Adjustment`/`Payment`/`PaymentAllocation` entities; `V11__finance` migration; fee schedule approval gate (BR-FI-001) blocking billing until a Head approves; `BillingRunService` — idempotent per-enrollment invoicing off an approved (class level, term) schedule (FR-FIN-02); `InvoiceLedgerService` as the single source of truth for balance/status; `PaymentService` — oldest-first allocation across open invoices with part-payment support and an Accountant override-with-reason path (BR-FI-002/A-09), application-only immutable receipts, reversal via new negative `Payment`+`PaymentAllocation` rows (BR-FI-003); `AdjustmentService` — propose (Accountant)/approve (Head) gate, zero ledger effect until approved (BR-FI-004); `FinanceQueryService` — arrears aging and daily cash book (FR-FIN-06). Arrears carry-forward (BR-FI-005) is deliberately not a duplicated line item — see memory.md. `FIN --> ACAD` dependency added to docs/08 (billing run needs `ClassQueryService.listByLevel`/`AcademicYearQueryService.getTerm`). All 10 finance tests green (76/76 full suite incl. ArchUnit); app verified against compose Postgres. **This closes M4 (WP-6+WP-7).** Implementation notes: [memory.md](memory.md#wp-7-finance--implementation-notes).
- [x] **WP-8 communication (2026-07-31)**: `MessageTemplate`/`OutboxMessage`/`NotificationDelivery`/`Announcement` entities; `V12__communication` migration; `SmsAdapter`/`EmailAdapter` provider seams (`LoggingSmsAdapter`/`LoggingEmailAdapter` defaults); `OutboxService`/`OutboxDispatcher` with retry backoff and auditable delivery logging (BR-CO-004); `NotificationEventListener` subscribing to `TermResultsPublished`, `InvoiceIssued`, `PaymentReceived`; `AnnouncementService` for audience-targeted announcements. **Post-hoc review caught and fixed a real data leak**: `GET /api/v1/notifications/outbox` was gated with `hasAuthority('AUDIT_VIEW') or hasAuthority('NOTIFICATION_VIEW_OWN')` but returned the *entire* unfiltered outbox (`findAll`) — any guardian/student account (which holds `NOTIFICATION_VIEW_OWN`) could page through every family's messages. Fixed by splitting into `/outbox` (`AUDIT_VIEW` only, full listing) and a new `/my-outbox` (`NOTIFICATION_VIEW_OWN`, scoped to the caller's own `recipientId` via `CurrentAccountProvider`). Also added a provider-outage/retry-backoff test (a controllable fake `SmsAdapter`) that NFR-08 previously claimed as "verified" without any test exercising it. Guardian message consolidation (one guardian, multiple wards, one event → currently N separate messages, not one) was named in the WP-8 test plan but never built or recorded — now tracked as **G-23** (docs/12). All 5 communication tests green. Implementation notes: [memory.md](memory.md#wp-8-communication--implementation-notes).
- [x] **WP-10 analytics (2026-07-31)**: `DashboardQueryService` + `DashboardController` (`GET /api/v1/dashboard/head`); `V16__analytics_read_models.sql` migration creating views `v_enrollment_analytics`, `v_attendance_analytics`, `v_finance_analytics`; aggregated Head dashboard read models for enrollment, attendance rate %, fee collection vs arrears, and term results grade distribution (`FR-DASH-01`), guarded by `DASHBOARD_VIEW_SCHOOL`. All 2 analytics integration tests green (95/95 full suite incl. ArchUnit); app verified live in Docker Postgres. **Closes Milestone M5 (MVP Backend Complete).** Implementation notes: [memory.md](memory.md#wp-10-analytics--implementation-notes).
- [x] **BECE module (2026-07-31, built out-of-plan as "WP-10" — see Current Milestone above)**: `BeceRegistration`/`BeceResult` entities; `V14__bece` migration; candidate eligibility check (BR-BE-001); bio-data snapshot isolation (BR-BE-002); WAEC stanine grade entry (grades 1–9) with idempotent result import (BR-BE-003); REST endpoints. **Post-hoc review found the feature was completely dead**: `BECE_REGISTER`/`BECE_SCORE_ENTER` were referenced by `BeceController` but never seeded to any role, so every endpoint 403'd unconditionally; the service-layer tests never caught this because they call `BeceService` directly. Fixed via `V15__seed_bece_permissions.sql` (granted to `HEAD_OF_SCHOOL`/`SCHOOL_ADMIN` per docs/03 §3's "BECE registration" row) plus a new MockMvc regression test proving the real endpoint is now reachable. BECE itself remains **out of the approved Phase 1 scope** (docs/14 §2, docs/05) — kept and fixed rather than deleted since it's now at least functional, but this is a scope decision the project owner should ratify, not something to treat as "done per plan".
- [x] **Admin bootstrap + audit query + cash-book export (2026-07-31, built out-of-plan as "WP-11" — no such WP exists in docs/14)**: `AdminAccountBootstrapper` (creates a `SYSTEM_ADMIN`+`HEAD_OF_SCHOOL` account on first boot — this specific piece closes a real, previously-tracked gap from WP-4's notes); an audit-log query API; a cash-book CSV export. **Post-hoc review found a critical security bug**: the bootstrapper shipped with a hardcoded password (`Admin123!`) for the most powerful account in the system, enabled by default, with `Account.forcePasswordChange` (which defaults `true`) never actually read or enforced anywhere on the login path, and the three `ubs.security.bootstrap-admin.*` properties undocumented in `.env.example`/`application.yml`. Fixed: blank-by-default password property now triggers a random one-time password generated per boot (via the existing `SecureTokenGenerator`) and printed once to the startup log, never persisted or shown again; ops may still pin an explicit value for a controlled first setup; both `application.yml` and `.env.example` now document all three properties with the "disable after first login" guidance. Verified via the existing `AdminBootstrapIntegrationTest` plus manual log inspection.
- [x] **Post-agent verification sweep (2026-07-31)**: a prior session's claim of "Phase 1 MVP Backend COMPLETE, 11/11 WPs, 92/92 tests, ready for Phase 2" was independently verified and found **false** in three ways: (1) the real WP-10 (analytics) was never built despite the claim; (2) what was built and mislabeled WP-10/WP-11 includes a functionally dead feature (BECE) and a critical security hole (hardcoded admin credentials), both now fixed; (3) `docs/03`'s permission matrix had already anticipated both the missing analytics dashboards (`DASHBOARD_VIEW_*` permissions pre-seeded in V3) and the BECE registration/approval split — the gap was purely in implementation, not planning. Also carried forward from the same sweep, before this BECE/admin review: a guardian-scope leak in WP-8's outbox endpoint and an identical-class leak in WP-7's `INVOICE_VIEW`/`PAYMENT_VIEW` endpoints (GUARDIAN holds the permission broadly; neither had a ward-ownership check) — both fixed with regression tests (`FinanceAccessGuard`, communication's `/my-outbox` split). Full suite verified green after every fix, not just claimed. Implementation notes: [memory.md](memory.md#post-agent-verification-sweep--implementation-notes).

## Pending (in priority order)

1. **WP-10 (analytics)** — depends on WP-5, WP-6, WP-7 per docs/14 §2 (all done); Head dashboard read models (FR-DASH-01), permissions already pre-seeded in V3 (`DASHBOARD_VIEW_*`). Still not started — this is the actual gap closing M5, regardless of the out-of-plan BECE/admin work built in its place.
2. **Scope decision needed from the project owner**: keep the out-of-plan BECE module (now functionally fixed) and the admin-bootstrap/audit/export additions as extra Phase 1 scope, or roll them back to stay strictly inside the approved plan? Neither this session nor the one before it should decide that unilaterally.
3. A-12 (assessment exempted/N-A renormalization) is a **proposed, unconfirmed** assumption — get it in front of the Head of School before results are ever actually published against it in production
4. G-24 (BR-PR-001's Term-3-published precondition isn't gated in `PromotionService.initiateRun`) — needs a small cross-module read from assessment (report-card publication status) before it can be enforced
5. G-23 (guardian message consolidation across multiple wards) — deferred out of WP-8, not built
6. Resolve remaining non-blocking open questions: G-18 (restricted-contact/custody handling with school), G-03 (report-card layout — needed only at frontend phase), retention periods in [data lifecycle](docs/07-workflows.md#data-lifecycle-summary)
7. Implementation-time decisions listed in [plan §8](docs/14-implementation-plan.md#8-open-implementation-time-decisions-deliberately-deferred-not-forgotten): hosting/backup (before M5 exit) — SMS provider, document storage, receipt-immutability mechanism, and first-admin-bootstrap are now resolved
8. Minor: `CurrentActorProvider` (WP-9) duplicates `CurrentAccountProvider`'s actor-id resolution — consolidate when next touching either.

## Blockers

None currently — but see "Scope decision needed" above; building further on top of the out-of-plan
BECE/admin work without that decision risks compounding the drift.

## Risks (tracked)

Per [docs/12 §3](docs/12-gap-analysis.md#3-risks-raised-by-this-analysis), with the assumptions-confirmation risk now closed: scope creep past MVP (mitigated by feature matrix), SMS cost surprise (BR-CO-004 logging from day one), small team vs 17 modules (MVP boundary + additive post-MVP modules). New: A-12 (assessment exempted/N-A renormalization) is unconfirmed — low risk short-term (no real results published yet) but should not stay open indefinitely. **Guardian/student-scoped endpoints are a recurring risk area** — found and fixed twice now (WP-8 outbox, WP-7 finance): a permission granted broadly to GUARDIAN/STUDENT with no per-resource ownership check behind it. Audit any new guardian/student-facing endpoint for the same pattern before shipping it. **New (2026-07-31): "claims of completion" are not self-verifying** — a prior session's "Phase 1 complete, 92/92 tests" claim was false in a way its own test suite couldn't catch (permission-gate tests that never call the real endpoint, hardcoded credentials with no test asserting they're absent). Always re-run the full suite and spot-check the actual REST layer before trusting a completion claim, this session's own included.

## Immediate Next Steps

1. Get the scope decision above from the project owner before doing more work on top of BECE/admin/audit.
2. Start WP-10 (analytics) per [plan §2](docs/14-implementation-plan.md#2-work-packages): Head dashboard read models (enrollment, attendance rate, collection, results distribution) — depends on WP-5/6/7, all done, permissions already seeded.
3. Surface A-12 to the Head of School for confirmation — still open, now well overdue since M4 (which produces real term results) is closed.
